#include <stdio.h>
int main()
{
    int sum = 0;
        printf(" number (0 to 10) ");
        do {
            printf("\n %d",sum);
        sum++;
       } while(sum<=10);
       return 0;
}